New: describe a meal in plain words and let AI estimate it — works alongside the photo scan (free Gemini key, set up in Settings). Tap into any meal for a full nutrition breakdown, with an optional per-meal line on the Day screen. The AI scan now explains how to activate it instead of silently falling back. Slimmer app: the old on-device photo model is gone.
